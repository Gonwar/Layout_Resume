mixin progress_dot(completed)
    ul.progress__dots
        - for (var i=0;i<12;i++)
            if(i<completed)
                li.progress__dots_dot.enabled
            else
                li.progress__dots_dot