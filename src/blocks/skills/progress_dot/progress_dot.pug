mixin progress_dot(completed)
    ul.progress-dots
        - for (var i=0;i<12;i++)
            if(i<completed)
                li.progress-dots__dot.progress-dots__dot_enabled
            else
                li.progress-dots__dot